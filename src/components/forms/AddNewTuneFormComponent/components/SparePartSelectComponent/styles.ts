import { baseTheme } from "@/components/constants";

export const styles = {
  selector: {
    margin: baseTheme.spacing(5),
  },

  outerBox: {
    display: "flex",
    flexFlow: "wrap",
    justifyContent: "center",
    marginTop: baseTheme.spacing(15),
  },
};
