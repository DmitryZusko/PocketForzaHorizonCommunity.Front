import { baseTheme } from "../constants";

export const styles = {
  outerContainer: { marginTop: baseTheme.spacing(7) },
  shareCodeBlock: { display: "flex", justifyContent: "center", alignItems: "center" },
};
