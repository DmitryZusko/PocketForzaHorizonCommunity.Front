import { baseTheme } from "@/components/constants";

export const styles = {
  outerContainer: {
    padding: baseTheme.spacing(10),
    maxWidth: { xs: "100vw", md: "75vw", lg: "35vw" },
    maxHeight: "80vh",
    overflowY: "auto",
  },
};
