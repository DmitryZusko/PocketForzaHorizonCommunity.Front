import { baseTheme } from "../constants";

export const styles = {
  outerContainer: {
    display: "flex",
    justifyContent: "space-around",
    alignItems: "center",
    marginTop: baseTheme.spacing(10),
  },
};
